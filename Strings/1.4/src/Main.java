public class Main {
    public static void main(String[] args) {

        String str = "121 dsd123sd 3290 903";
        int count = 0;

        for (int i = 0; i < str.length(); i++) {
            if (Character.isDigit(str.charAt(i))) {
                for (int j = i; ; j++) {
                    if (str.charAt(j) = " " || Character.isDefined(str.charAt(j))) {

                    }
                }
            }
        }

    }
}
